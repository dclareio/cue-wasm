{
	"name": "cue-wasm",
  "version": "0.0.1",
  "main": "lib/index.js",
  "devDependencies": {
    "jest": "^28.1.1"
  },
  "scripts": {
    "build:wasm": "DOCKER_BUILDKIT=1  docker build -o lib -f Dockerfile .",
    "test": "jest"
  }
}
